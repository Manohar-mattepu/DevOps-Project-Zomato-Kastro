apiVersion: v1
kind: Service
metadata:
  name: zomato-service
  labels:
    app: zomato
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 32000  # Replace with the port your app runs on
  selector:
    app: zomato
